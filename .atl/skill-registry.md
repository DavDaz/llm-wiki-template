# Skill Registry — llm-wiki-generator

Generated by `sdd-init` on 2026-04-24. Regenerate via `/sdd-init` or the `skill-registry` skill.

## Project Conventions
- `CLAUDE.md` (project root) — llm-wiki project context, release workflow, Bubbletea huh binding note
- `~/.claude/CLAUDE.md` (user global) — rules, personality, skill auto-load table

## User Skills (relevant to this stack)

| Skill | Location | Triggers |
|-------|----------|----------|
| golang-pro | `~/.claude/skills/golang-pro/SKILL.md` | Go code, concurrency, goroutines, channels |
| go-testing | `~/.claude/skills/go-testing/SKILL.md` | Go tests, including Bubbletea TUI testing patterns |
| sdd-* | `~/.claude/skills/sdd-*/SKILL.md` | SDD workflow phases |
| skill-creator | `~/.claude/skills/skill-creator/SKILL.md` | Creating new skills |

## Compact Rules (auto-inject into sub-agents)

### Go (from golang-pro + go-testing)
- Prefer standard library; pull deps only when they provide real value
- Errors: wrap with `fmt.Errorf("context: %w", err)`; never swallow
- Package naming: short, lowercase, singular (`manifest`, not `manifests`)
- Tests: table-driven via `[]struct{name string; ...}` + `t.Run(tc.name, ...)`
- Test files: `*_test.go` alongside source; use `testify/require` for hard asserts, `testify/assert` for soft
- Bubbletea tests: use `teatest` or drive `Update(msg)` directly and assert on `View()` — no need for actual terminal

### Project-specific rules (from CLAUDE.md)
- NEVER build after code changes (user rule). Validate via `go test -race ./...` only.
- NEVER use cat/grep/find/sed/ls in Bash. Use bat/rg/fd/sd/eza.
- Commits: conventional commits, NO AI attribution. Example: `feat: add pages parser`, `fix: preserve frontmatter comments on save`.
- Huh form values → MUST be `*formValues` heap-allocated pointer (Bubbletea Model passed by value).
- Language: Rioplatense Spanish for user-facing messages when relevant; English for code/comments.

### Testing rules
- Strict TDD Mode: ENABLED. Write failing test first, then implementation.
- Test command: `go test -race ./...` (or `make test`).
- Linter: `golangci-lint run ./...` (or `make lint`).
